# إعداد Railway — هدايات

الموقع لا يعتمد على Netlify بعد الآن.

## متغيرات البيئة الموصى بها

- `ADMIN_PASSWORD`: كلمة مرور قوية للوحة `/admin/` (مطلوبة لتفعيل اللوحة).
- `ADMIN_SECRET`: مفتاح جلسة مختلف وطويل (اختياري؛ عند عدم وضعه يستخدم ADMIN_PASSWORD).
- `DATA_DIR=/data`: استخدمه عند ربط Railway Volume بالمسار `/data` حتى تبقى رسائل التواصل وآراء الزوار وقياسات الرضا وطلبات المستفيدين وتعديلات المحتوى بعد إعادة النشر.

## إرسال رسائل التواصل إلى البريد (اختياري)

النموذج يعمل ويحفظ الرسائل في الخادم حتى بدون مزود بريد. ولإرسال نسخة إلى البريد عبر Resend أضف:

- `RESEND_API_KEY`
- `CONTACT_FROM_EMAIL` (عنوان مرسل موثّق لدى Resend)
- `CONTACT_TO_EMAIL` (البريد المستلم؛ إذا لم يوضع يحاول استخدام البريد الموجود في site.json)

## ملاحظات

- الخادم يدعم HTTP Range و`video/mp4` لتشغيل الفيديو والتنقل داخله على Safari/iPhone والمتصفحات الحديثة.
- الروابط المباشرة مثل `/#contact` و`/#about` محفوظة ولا يتم حذفها عند فتح الصفحة.

- يتم حفظ تقييمات «شاركنا رأيك» في `feedback-messages.jsonl` داخل `DATA_DIR`، وتظهر في تبويب «تقييم الصفحات» داخل لوحة الإدارة.
- يتم حفظ قياسات الرضا في `satisfaction-responses.jsonl`.
- يتم حفظ طلبات مركز مشاركة المستفيد في `participation-requests.jsonl`، وتحديثات الحالات في `participation-updates.jsonl`.
- كل طلب مشاركة يحصل على رقم مرجعي بصيغة `HDY-YYYY-XXXXXXXXXX` ويمكن للمستفيد متابعة حالته من صفحة مركز المشاركة.
